import styled from 'styled-components';

const SectionStartDate = styled.div`
    position: relative;
    float: left;
    margin-left: 10px;
    margin-right: 4px;
    color: red;
`;

export default SectionStartDate;