import styled from 'styled-components';

const ImageWrapper = styled.div`
    position: relative;
    float: left;
    top: -20px;
    width: 50%;
    height: inherit;
`;

export default ImageWrapper;