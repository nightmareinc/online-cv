import styled from 'styled-components';

const PassionList = styled.div`
    width: 100%;
    position: relative;
    float: left;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`;

export default PassionList;