import styled from 'styled-components';
import Card from '../../common/Card';
import Title from '../../common/Card/Title';
import SectionTitle from '../../common/Card/SectionTitle';
import SectionStartDate from '../../common/Card/SectionStartDate';
import SectionEndDate from '../../common/Card/SectionEndDate';
import SectionSubTitle from '../../common/Card/SectionSubTitle';
import SectionDescription from '../../common/Card/SectionDescription';
import SectionListWrapper from '../../common/Card/SectionListWrapper';

const ExperienceCard = styled(Card)`
    
`;

export {
    ExperienceCard as default,
    Title,
    SectionTitle,
    SectionStartDate,
    SectionEndDate,
    SectionSubTitle,
    SectionDescription,
    SectionListWrapper
};