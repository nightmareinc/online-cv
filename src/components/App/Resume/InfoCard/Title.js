import styled from 'styled-components';

const Title = styled.div`
    font-size: 20px;
    direction: ltr;
    font-weight: 600;

    &::after {
        content: ':';
    }

`;

export default Title;